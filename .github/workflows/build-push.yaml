name: Build and Push Docker Image

on:
  push:
    branches: [ main, master ] # Trigger on pushes to main/master branches
  pull_request:
    branches: [ main, master ] # Also trigger on PRs to these branches (tests will run, push won't)
  workflow_dispatch: # Allows manual triggering from the Actions tab

jobs:
  test:
    name: Run on Ubuntu
    runs-on: ubuntu-latest
    steps:
      - name: Clone the code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Running Tests
        run: |
          go mod tidy
          make test

  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    needs: test 
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU (for multi-arch builds, optional but good practice)
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push with Makefile
        env:
          IMG: ${{ secrets.DOCKERHUB_USERNAME }}/application-lifecycle-manager:latest
        run: |
          make docker-build
          make docker-push